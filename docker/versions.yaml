# Version configuration file - centralized management of all software versions
versions:
  # Base images
  python: "python:3.14-slim-bookworm"
  golang: "1.25.0"
  nodejs: "v20.20.0"

  # Python packages (unified configuration)
  python_packages: "httpx==0.27.2 requests==2.33.0 jinja2==3.1.6 PySocks httpx[socks] numpy pandas thefuzz pydantic matplotlib scikit-learn python-docx markdown2 beautifulsoup4 openpyxl py3Fdfs pymysql gmssl==3.2.2"

  # System packages
  system_packages:
    - pkg-config
    - libseccomp-dev
    - wget
    - curl
    - xz-utils
    - zlib1g
    - expat
    - perl
    - libsqlite3-0

  # Build dependencies
  build_packages:
    - pkg-config
    - gcc
    - libseccomp-dev

# Mirror configuration
mirrors:
  nodejs: "https://nodejs.org/dist"
  golang: "https://golang.org/dl"

# Architecture-specific configuration
architectures:
  amd64:
    golang_arch: "linux-amd64"
    nodejs_arch: "linux-x64"
  arm64:
    golang_arch: "linux-arm64"
    nodejs_arch: "linux-arm64"
